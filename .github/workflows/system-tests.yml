name: System Tests

on:
  workflow_call:
    inputs:
      image-tags:
        description: Space or newline-separated service=tag pins or exact image refs from deploy-config-schema lock images.
        required: false
        default: ''
        type: string
  workflow_dispatch:
    inputs:
      image-tags:
        description: Space or newline-separated service=tag pins or exact image refs from deploy-config-schema lock images.
        required: false
        default: ''
        type: string

permissions:
  contents: read
  packages: read

jobs:
  validate-image-tags:
    name: Validate IMAGE_TAGS
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Validate image ref contract
        if: ${{ inputs.image-tags != '' }}
        env:
          IMAGE_TAGS: ${{ inputs.image-tags }}
        run: scripts/validate-image-tags.sh --require-all

      - name: Skip absent image tags
        if: ${{ inputs.image-tags == '' }}
        run: echo 'IMAGE_TAGS not provided; live system tests will be skipped.'

  non-playwright:
    name: Non-Playwright System Tests
    runs-on: ubuntu-latest
    needs: [validate-image-tags]
    if: ${{ inputs.image-tags != '' }}
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - uses: gradle/actions/setup-gradle@v6

      - name: Run non-Playwright tests
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ github.token }}
          IMAGE_TAGS: ${{ inputs.image-tags }}
        run: ./gradlew :system-tests:testNonPlaywright --no-daemon

  playwright:
    name: Playwright System Tests
    runs-on: ubuntu-latest
    needs: [validate-image-tags]
    if: ${{ inputs.image-tags != '' }}
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4]
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - uses: gradle/actions/setup-gradle@v6

      - name: Run Playwright shard
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ github.token }}
          IMAGE_TAGS: ${{ inputs.image-tags }}
        run: >-
          ./gradlew :system-tests:testPlaywright --no-daemon
          -Dtest.shard.index=${{ matrix.shard }}
          -Dtest.shard.count=4

  pipeline-complete:
    name: Pipeline Complete
    runs-on: ubuntu-latest
    needs: [validate-image-tags, non-playwright, playwright]
    if: ${{ always() }}
    steps:
      - name: Check required jobs
        uses: re-actors/alls-green@release/v1
        with:
          allowed-skips: non-playwright, playwright
          jobs: ${{ toJSON(needs) }}
